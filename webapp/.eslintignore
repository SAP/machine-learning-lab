# Exclude this folder since it is autogenerated with swagger
src/services/client

# Ignore the src/ folder for now so that eslint does not block the building process. The IDE, e.g. VS Code, should still show the errors. Refactor step-by-step
src/
